enum ServiceStatus {
  initial,
  loading,
  loadingSuccess,
  loadingFailure,
  submiting,
  submissionSucess,
  submissionFailure
}

enum AuthenticationStatus { unkown, authenticated, unAuthenticated }
